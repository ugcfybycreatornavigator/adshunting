import { Activity, CalendarDays, Clock3, Layers3 } from "lucide-react";
import { Card } from "@/components/ui";
import type { BrandData } from "@/lib/brand-data";
import { formatDate } from "@/lib/utils";

export function BrandSummary({data}:{data:BrandData}){return <><div className="mt-8 grid grid-cols-2 gap-3 lg:grid-cols-4"><Metric icon={<Activity/>} label="Active ads" value={data.active}/><Metric icon={<Layers3/>} label="Total creatives" value={data.total}/><Metric icon={<Clock3/>} label="Longest running" value={`${data.longest} days`}/><Metric icon={<CalendarDays/>} label="Earliest tracked" value={data.earliest?formatDate(data.earliest):"Not available"}/></div><div className="mt-6 grid gap-4 lg:grid-cols-3"><Breakdown title="Media distribution" values={data.media}/><Breakdown title="CTA distribution" values={data.ctas}/><Breakdown title="Platform distribution" values={data.platforms}/></div></>}
function Metric({icon,label,value}:{icon:React.ReactNode;label:string;value:string|number}){return <Card className="p-4 shadow-none"><span className="text-signal [&>svg]:size-4">{icon}</span><p className="mt-4 text-2xl font-semibold tracking-tight">{value}</p><p className="mt-1 text-xs text-muted">{label}</p></Card>}
function Breakdown({title,values}:{title:string;values:Record<string,number>}){const entries=Object.entries(values).sort((a,b)=>b[1]-a[1]);const total=entries.reduce((sum,[,value])=>sum+value,0);return <Card className="p-5 shadow-none"><h3 className="text-sm font-semibold">{title}</h3>{entries.length?<div className="mt-5 space-y-4">{entries.slice(0,5).map(([name,value])=><div key={name}><div className="flex justify-between text-[11px]"><span className="capitalize text-muted">{name.replaceAll("_"," ")}</span><span className="font-semibold">{Math.round(value/total*100)}%</span></div><div className="mt-1.5 h-1.5 rounded-full bg-surface"><div className="h-full rounded-full bg-signal" style={{width:`${value/total*100}%`}}/></div></div>)}</div>:<p className="mt-5 text-xs leading-5 text-muted">Distribution appears after this brand’s ads are stored in the catalogue.</p>}</Card>}
