"use client";

import { useState } from "react";
import { ExternalLink, Globe2, Loader2, Search } from "lucide-react";
import { Button, Card } from "@/components/ui";

type Result={title:string;url:string;displayUrl:string;snippet:string};
export function BrandWebIntelligence({brandName,configured}:{brandName:string;configured:boolean}){const[results,setResults]=useState<Result[]>([]);const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function enrich(){setBusy(true);setError("");try{const response=await fetch("/api/search/google",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({query:`${brandName} official website`})});const data=await response.json();if(!response.ok)throw new Error(data.message||"Web enrichment failed.");setResults(data.results);}catch(error){setError(error instanceof Error?error.message:"Web enrichment failed.");}finally{setBusy(false)}}return <Card className="mt-6 p-5 shadow-none"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><Globe2 size={16} className="text-brand"/><h2 className="text-sm font-semibold">Google web intelligence</h2></div><p className="mt-1 text-xs text-muted">Optional, clearly separated website and brand-domain enrichment.</p></div><Button variant="secondary" onClick={enrich} disabled={!configured||busy}>{busy?<Loader2 className="animate-spin" size={15}/>:<Search size={15}/>} {configured?"Enrich brand":"Not configured"}</Button></div>{error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-xs text-signal">{error}</p>}{results.length>0&&<div className="mt-5 divide-y divide-line border-t border-line">{results.map(result=><a key={result.url} href={result.url} target="_blank" rel="noreferrer" className="flex gap-3 py-4 hover:text-brand"><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{result.title}</p><p className="mt-1 truncate text-[11px] text-muted">{result.displayUrl}</p><p className="mt-2 line-clamp-2 text-xs leading-5 text-muted">{result.snippet}</p></div><ExternalLink size={15} className="shrink-0"/></a>)}</div>}</Card>}
